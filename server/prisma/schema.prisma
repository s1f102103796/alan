datasource db {
  provider = "postgresql"
  url      = env("API_DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Job {
  id          String   @id
  description String
  status      String
  createdAt   DateTime
}
